## What does this PR do?

<!-- One or two sentences. -->

## Why?

<!-- What problem does this solve, or what does it enable? -->

## How was this tested?

<!-- Real commands and real output, not "should work". E.g.:
- `cargo test --release --all-features`
- `pytest tests/ -v`
-->

## Checklist

- [ ] Tests added/updated for the change (or explained why not needed)
- [ ] `cargo test --release --all-features` passes (if Rust code changed)
- [ ] `pytest tests/` passes (if Python code changed)
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] Docs updated if behavior or public API changed
- [ ] No new hardcoded secrets/credentials
